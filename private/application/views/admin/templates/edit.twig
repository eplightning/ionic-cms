<h2>Edycja szablonu</h2>
{% if not writable %}
<div class="ui-widget" style="margin-top: 10px">
	<div class="ui-state-highlight ui-corner-all" style="padding: 15px; margin-bottom: 10px">
		<span class="ui-icon ui-icon-info" style="float: left; margin-right: 15px"></span> Aby edytować ten plik w panelu admina, musisz nadać mu odpowiednie uprawnienia (CHMOD 666/777)
	</div>
</div>
{% endif %}
<div class="section">
	<div class="theader">
		<div class="theader2">
			Informacje
		</div>
	</div>
	<div class="section-content">
		<div class="elem">
			<label>Tytuł</label>
			<div class="right">{{ item.name }}</div>
		</div>
	</div>
</div>
<div class="section">
	<div class="theader">
		<div class="theader2">
			Szablon
		</div>
	</div>
	<div class="section-content">
		<form action="admin/templates/edit/{{ item.id }}" method="post">
			<div class="elem">
				<textarea id="code" name="code">{{ content|e }}</textarea>
			</div>
			<div class="toolbar ui-widget-header ui-corner-all">
				{{ form_token() }}
				<input type="submit" name="submit" style="width: auto" value="Zapisz" />
			</div>
		</form>
	</div>
</div>
<script type="text/javascript">
$(function(){
	var editor = CodeMirror.fromTextArea(document.getElementById("code"), {mode: "text/twig", tabMode: "indent", lineNumbers: true});
	editor.setSize(null, 700);
	$('input[type="submit"]').button();
});
</script>